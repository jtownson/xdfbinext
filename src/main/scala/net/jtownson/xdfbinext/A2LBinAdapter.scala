package net.jtownson.xdfbinext

import java.io.File

/**
 * What would be nice here would be to convert an a2l+bin to some kind of
 * repr where we are able to answer questions such as 'find axes where
 * the units are kg/h and the values are below 1300 kg/h.
 * */
class A2LBinAdapter(val bin: File, a2l: A2LWrapper) {
  
  
}
